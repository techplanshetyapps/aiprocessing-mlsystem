name: aiprocessing_mlsystem
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  dio: ^5.4.0

flutter:
  uses-material-design: true
  generate: true 
  assets:
    - assets/data/employee_professions.json
